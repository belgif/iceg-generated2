{
  "title": "PipeType",
  "type": "object",
  "description": "The type of pipe.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse PipeType)",
      "pattern": "^PipeType$"
    }
  },
  "required": [
    "@type"
  ]
}
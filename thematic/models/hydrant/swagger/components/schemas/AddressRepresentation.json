{
  "title": "AddressRepresentation",
  "type": "object",
  "description": "More readable representation with only the basic data of the address, intended to use an address as an attribute of another object.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse AddressRepresentation)",
      "pattern": "^AddressRepresentation$"
    },
    "houseNumber": {
      "type": "object",
      "description": "Alphanumeric code officially assigned to building units, berths, pitches or parcels.",
      "properties": {
        "@value": {
          "type": "string"
        }
      },
      "required": [
        "@value"
      ]
    },
    "postalcode": {
      "type": "object",
      "description": "Code designating the geographical area that groups addresses for postal purposes.",
      "properties": {
        "@value": {
          "type": "string"
        }
      },
      "required": [
        "@value"
      ]
    },
    "streetname": {
      "$ref": "#/components/schemas/GeographicalName"
    },
    "municipalityName": {
      "$ref": "#/components/schemas/GeographicalName"
    }
  },
  "required": [
    "@type"
  ]
}
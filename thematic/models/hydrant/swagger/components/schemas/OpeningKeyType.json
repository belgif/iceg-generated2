{
  "title": "OpeningKeyType",
  "type": "object",
  "description": "The type of a metal instrument by which the bolt of a lock is turned.",
  "properties": {
    "@id": {
      "type": "string",
      "format": "uri"
    },
    "@type": {
      "type": "string",
      "description": "Object type (klasse OpeningKeyType)",
      "pattern": "^OpeningKeyType$"
    }
  },
  "required": [
    "@type"
  ]
}